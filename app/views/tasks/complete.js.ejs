$('#task_<%=task._id%>').remove();
$('#completed_tasks').prepend("<%-  partial('tasks/_completed_task.ejs', {collection:[task], as: 'task'}).replace(/"/gi,"\\\"").replace(/\n/gi, "\\n") %>");
$('#task_<%=task._id%>').effect("highlight", {}, 2000);